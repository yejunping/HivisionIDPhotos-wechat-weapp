name: Package to JAR and Create Release

on:
  # 支持手动触发
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag (e.g., v1.0.0)'
        required: true
        default: 'v1.0.0'
      release_name:
        description: 'Release name'
        required: true
        default: 'Release v1.0.0'

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code from specific repository
        uses: actions/checkout@v3
        with:
          repository: yejunping/HivisionIDPhotos-wechat-weapp # 替换为你实际要拉取代码的仓库地址

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '8'
          distribution: 'temurin'
          cache: maven

      - name: Build project with Maven
        run: mvn -B clean package

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.event.inputs.release_tag }}
          release_name: ${{ github.event.inputs.release_name }}
          draft: false
          prerelease: false

      - name: Upload JAR as Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: target/HivisionIDPhotos-wechat-weapp.jar # 替换为实际生成的 JAR 文件路径
          asset_name: HivisionIDPhotos-wechat-weapp.jar # 替换为实际生成的 JAR 文件名
          asset_content_type: application/java-archive
